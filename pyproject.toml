[project]
name = "call-me-maybe"
version = "0.1.0"
description = "42 Call me maybe"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.6",
    "pydantic>=2.13.4",
    "llm-sdk",
]

[tool.uv.sources]
llm-sdk = { path = "./llm_sdk", editable = true }

[dependency-groups]
dev = [
    "flake8>=7.3.0",
    "mypy>=2.1.0",
]
